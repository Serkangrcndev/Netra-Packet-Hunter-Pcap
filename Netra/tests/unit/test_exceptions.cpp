#include <gtest/gtest.h>

#include "netra/error/Exceptions.hpp"

using namespace netra;

TEST(ExceptionsTest, BasicException) {
    // Test exception throwing
    EXPECT_TRUE(true);
}

TEST(ExceptionsTest, ExceptionMessage) {
    // Test exception message
    EXPECT_TRUE(true);
}
