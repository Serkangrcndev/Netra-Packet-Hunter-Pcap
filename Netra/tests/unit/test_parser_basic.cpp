#include <gtest/gtest.h>

#include "netra/parser/PacketParser.hpp"

using namespace netra;

TEST(ParserTest, BasicCreation) {
    // Test parser initialization
    PacketParser parser;
    EXPECT_TRUE(true);
}

TEST(ParserTest, EmptyPacket) {
    // Test empty packet handling
    EXPECT_TRUE(true);
}
