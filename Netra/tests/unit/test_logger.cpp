#include <gtest/gtest.h>

#include "netra/logging/Logger.hpp"

using namespace netra;

TEST(LoggerTest, BasicCreation) {
    // Test logger initialization
    EXPECT_TRUE(true);
}

TEST(LoggerTest, LogLevelTest) {
    // Test log level functionality
    EXPECT_TRUE(true);
}
